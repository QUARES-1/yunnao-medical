server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://rm-cn-eh54ubjz5000196o.rwlb.rds.aliyuncs.com:3306/cloud_brain_diagnosis?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: lrm2
    password: 1524797479Li!
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# JWT密钥配置（生产环境请通过环境变量 JWT_SECRET 覆盖）
jwt:
  secret: ${JWT_SECRET:cloud-brain-diagnosis-secret-key-2026-abcdefghijklmnopqrstuvwxyz}

# 微信小程序配置（生产环境请通过环境变量覆盖）
wechat:
  appid: wx64e4374dc4f3c242
  secret: ${WECHAT_SECRET:${WECHAT_APP_SECRET:}}

# 文件上传路径
file:
  upload:
    path: D:/upload/

# Knife4j配置
knife4j:
  enable: true
  openapi:
    title: 云脑诊疗平台API文档
    description: 传统业务后端接口
    version: 1.0.0

# 日志配置
logging:
  level:
    com.neusoft.cloud_brain_diagnosis: debug
    org.hibernate.SQL: debug