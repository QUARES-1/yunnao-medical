server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_brain_diagnosis?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "gaosihan2006"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: ${JWT_SECRET:cloud-brain-diagnosis-secret-key-2026-abcdefghijklmnopqrstuvwxyz}

wechat:
  appid: wx64e4374dc4f3c242
  secret: ${WECHAT_APP_SECRET:}

file:
  upload:
    path: D:/upload/

knife4j:
  enable: true
  openapi:
    title: 云脑诊疗平台API文档
    description: 传统业务后端接口
    version: 1.0.0

logging:
  level:
    com.neusoft.cloud_brain_diagnosis: debug
    org.hibernate.SQL: debug
